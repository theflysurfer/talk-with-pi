## Parent

#1 (spec Pont vocal Pi)

## What to build

Recevoir un `user_text` sur le WebSocket et l'injecter dans la session Pi vivante via `pi.sendUserMessage`, en followUp par défaut (steer en commande vocale explicite à part), avec accusé de réception (state working). Le message apparaît dans pi-web comme un message normal, sans enveloppe, source distinguable (extension).

## Acceptance criteria

- [ ] wscat envoie `user_text` → le message arrive dans la session Pi telle quelle
- [ ] Pi occupé : le message part en followUp (file), ne coupe pas le run
- [ ] Accusé : state phase=working (ou error si l'injection échoue)
- [ ] Le message apparaît dans pi-web sans doublon ni instruction ajoutée

## Blocked by

#2 Verrou + serveur WS base

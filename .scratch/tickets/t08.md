## Parent

#1 (spec Pont vocal Pi)

## What to build

Relais VPS → PC : vhost nginx `talk.srv759970.hstgr.cloud` sur le VPS (cert LE existant), reverse-proxy vers le PC via ZeroTier (`10.77.208.239:<port>`), page de status. Seule pièce d'infra à ajouter à l'existant.

## Acceptance criteria

- [ ] `https://talk.srv759970.hstgr.cloud` répond depuis le Pixel en 4G
- [ ] Le VPS relaie au PC via le mesh ZeroTier (aucun cert à gérer côté PC)
- [ ] Service protégé (HTTP basic-auth + token)

## Blocked by

None — prérequis P0, peut démarrer immédiatement.

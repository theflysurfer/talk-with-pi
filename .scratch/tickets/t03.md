## Parent

#1 (spec Pont vocal Pi)

## What to build

Sous-titres : écouter `message_update` (text_delta) pendant un run Pi, segmenter le texte en phrases et émettre des `say` au fil de l'eau (voix Pi). Nettoyage oral : blocs de code/tableaux/diffs → marqueur + descripteur ; chemin → nom de base ; URL → domaine ; puces → un item = une phrase ; marqueurs markdown retirés ; secrets masqués (« (secret masqué) »). Jamais de troncature — coupure à l'oreille par barge-in.

## Acceptance criteria

- [ ] Déclencher un run Pi → des `say` segmentés arrivent sur le WS au fil du streaming
- [ ] Bloc de code rendu « (bloc de code, N lignes) » et envoyé au descripteur, pas dit cru
- [ ] chemin `src/auth/login.ts` → « login.ts » ; URL → domaine seul
- [ ] Un secret (clé/token/mot de passe) visible dans la sortie Pi → jamais émis à l'oral
- [ ] La réponse complète est lue (pas de troncature)

## Blocked by

#2 Verrou + serveur WS base

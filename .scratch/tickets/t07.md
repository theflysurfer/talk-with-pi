## Parent

#1 (spec Pont vocal Pi)

## What to build

Voix d'entrée : l'audio Micro du client arrive sur le WebSocket, passe par `/stt` de Pi Daemon (provider Groq whisper) et devient un `user_text` injecté dans Pi (réutilise le ticket #2). Réutilise l'endpoint existant, aucun réimplémentation STT.

## Acceptance criteria

- [ ] Jeter un audio au WS → le texte transcrit (FR, termes techniques anglais compris) est injecté dans Pi
- [ ] Passe par /stt de Pi Daemon (provider Groq), pas un nouvel STT
- [ ] Erreur STT → error, pas de silence muet

## Blocked by

#3 user_text → injection Pi
